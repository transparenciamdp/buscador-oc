{
  "name": "mgp_buscadoroc",
  "version": "1.0.0",
  "description": "Buscador de Ordenes de Compra",
  "main": "dist/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node node_modules/json-server/bin --watch fake-api/data.json --routes fake-api/routes.json -s dist"
  },
  "author": "http://github.com/tincho",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.10.3"
  }
}
